#include <iostream>
using namespace std;
int gcd(int a, int b){
    while (b) {
        a = a % b;
        swap(a,b);
        
    }
    return a;
    
}
int main(){
    int x,y;
    cin >> x >> y;
    cout << gcd(x,y) << endl;
    return 0;
    
}
